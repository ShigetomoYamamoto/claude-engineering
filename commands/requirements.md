---
description: 曖昧な要望を構造化された要件に整理します。requirements-analyst エージェントを起動し、機能要件・非機能要件・スコープ・受け入れ基準を文書化します。
---

# 要件分析

**requirements-analyst エージェント**を起動して、自由形式の要望を実装可能な要件に変換します。
手順・出力・承認待ちの挙動はすべて **エージェント本体が単一の正**(このコマンドは薄い起動器で、内容を二重定義しない。ADR-014 / 監査 P5)。

## いつ使うか

- 全自動モードの起点として(要件 → 設計 → 実装 → デプロイ)
- 「〇〇を作りたい」「△△機能が欲しい」などの自由形式の要望を受けたとき

## 次のステップ

要件確定後: `/design`(設計が必要な場合)または `/plan`(既存設計の範囲内)。

承認された要件は `docs/requirements.md` に永続化される(セッションを跨いで残す正本。書き込みはメインが思考ティア
(Opus/Fable)へエスカレーション中は `opus-execution-guard` でブロックされるため executor へ委譲する — 詳細は
agent 本体「Persist on Approval」)。

## 関連エージェント

`~/.claude/agents/requirements-analyst.md` を起動します(要件をユーザーが承認するまで設計・実装に進みません — 詳細はエージェント本体)。
