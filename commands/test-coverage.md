---
description: テストカバレッジを分析して 80% 以上に達するための不足テストを生成します。tdd-guide エージェントを起動してカバレッジのギャップを特定・補完します。
---

# テストカバレッジ補完

**tdd-guide エージェント**を起動して、カバレッジを分析し不足しているテストを生成します。

> **位置づけ:** 「カバレッジ80%」は独立した段ではなく、**コードを作る段の完了条件の一部**
> (`loop-engineering` の完了判定 / `~/.claude/rules/testing.md`)。`/test-coverage` はその完了条件を
> 後から補うための補助コマンドで、tdd-guide を呼ぶ。新規実装では loop-engineering の完了判定の
> 中でカバレッジを満たすのが本筋(ADR-014)。

## このコマンドが行うこと

- `npm test --coverage` でカバレッジを計測する
- 80% を下回っているファイルを特定する
- カバーされていないパスに対してユニット・統合・E2E テストを生成する
- 新しいテストがパスすることを確認する
- 補完前後のカバレッジ数値を報告する

## いつ使うか

- カバレッジが 80% を下回ったとき
- テストなしで新機能を追加した後
- リリース前にカバレッジ目標を確認するとき

## 関連エージェント

`~/.claude/agents/tdd-guide.md` を起動します。
